'use client'

import React from 'react'
import { ModulePlaceholder } from '../shared/ModulePlaceholder'

const Permissions: React.FC = () => (
  <ModulePlaceholder title="Permission Layers" emoji="🔒" hue={330} />
)

export default Permissions
