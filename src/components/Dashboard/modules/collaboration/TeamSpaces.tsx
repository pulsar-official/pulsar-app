'use client'

import React from 'react'
import { ModulePlaceholder } from '../shared/ModulePlaceholder'

const TeamSpaces: React.FC = () => (
  <ModulePlaceholder title="Team Spaces" emoji="👥" hue={330} />
)

export default TeamSpaces
