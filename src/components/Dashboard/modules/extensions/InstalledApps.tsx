'use client'

import React from 'react'
import { ModulePlaceholder } from '../shared/ModulePlaceholder'

const InstalledApps: React.FC = () => (
  <ModulePlaceholder title="Installed Apps" emoji="🧩" hue={20} />
)

export default InstalledApps
